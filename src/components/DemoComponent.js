import React from 'react';

const DemoComponent = () => {
  return (
    <div style={{ textAlign: 'center', padding: '2rem', backgroundColor: '#f0f0f0' }}>
      <h1>Reusable Component from Local Package</h1>
      <p>This component is shared from a local npm package!</p>
    </div>
  );
};

export default DemoComponent;
